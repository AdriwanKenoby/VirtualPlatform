version: 2
ethernets:
  default:
    match:
      driver: virtio*   # Match n'importe quelle interface virtio
    dhcp4: true   
    dhcp6: false
    routes:
      - to: default
        via: ${gateway}
    nameservers:
      addresses:
        - 192.168.1.254
